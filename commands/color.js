module.exports = {
    
    name: 'color',
  çalıştır: async (client, message, args,prefix ) => {
    
    const { MessageActionRow, MessageButton } = require('discord.js');
    const babansramtanmaddox = require('../sramtan.json')
    
    
    xxxxxxx 10 Star Sonra Sizlerle
